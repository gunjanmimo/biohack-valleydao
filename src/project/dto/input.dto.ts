export interface ProjectCreationDTO {
  title: string;
  summary: string;
  methodology: string;
  environmentalImpact: string;
}

export interface ProjectSelectionDTO {
  name: string;
  value: string;
}
